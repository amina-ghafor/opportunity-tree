# Pinned deliberately light. Source adapters use the standard library for
# HTTP; openpyxl is here only because the AI Incident Database publishes its
# joined export as .xlsx. The anthropic SDK will follow once classification
# is built. pandas has not yet earned its place over plain dataclasses.
openpyxl
